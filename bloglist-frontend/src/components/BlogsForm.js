import React, { useState } from 'react'

const BlogsForm = ({setBlogs, blogs}) => {

    const [newBlog, setNewBlog] = useState({});


  return (
    <div>BlogsForm</div>
  )
}

export default BlogsForm